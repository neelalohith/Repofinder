package com.example.repofinder.ui.theme

import androidx.compose.foundation.shape.CircleShape
import androidx.compose.foundation.shape.RoundedCornerShape
import androidx.compose.material3.Shapes
import androidx.compose.ui.unit.dp

val Shapes = Shapes(
    small = RoundedCornerShape(4.dp),  // For small elements (e.g., buttons)
    medium = RoundedCornerShape(8.dp), // Medium elements (e.g., cards)
    large = RoundedCornerShape(16.dp)  // Large elements (e.g., containers)
)